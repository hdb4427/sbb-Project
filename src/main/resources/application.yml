server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      force: true

spring:
  application:
    name: health

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  devtools:
    livereload:
      enabled: true
      port: 35729
    restart:
      enabled: true

  thymeleaf:
    cache: false
  web:
    resources:
      cache:
        period: 0
      chain:
        cache: false

  datasource:
    url: jdbc:mysql://weare-db.czcemigaq5ac.ap-southeast-2.rds.amazonaws.com/weare_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: root
    password: aZLJ40dm5gb3eh4GSijm
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        # [수정 3] MariaDBDialect -> MySQL8Dialect (MySQL 사용 시 권장)
        dialect: org.hibernate.dialect.MySQL8Dialect

  # [중요 수정] 이 부분이 'false'면 JPA 저장소를 못 찾아서 프로그램이 죽습니다.
  # 원본 유지 차원에서 주석 처리하거나, 아예 지워버려야 합니다.
  # data:
  #   jpa:
  #     repositories:
  #       enabled: false

  security:
    user:
      name: test
      password: 1111
      roles: USER

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
        orm:
          jdbc:
            bind: trace